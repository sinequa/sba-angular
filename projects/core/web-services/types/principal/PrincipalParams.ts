
export type PrincipalParams = {
    offset?: number, // 0
    limit?: number, // 10
    isUser?: boolean, // true
    isGroup?: boolean, // true
    search?: string // search by name, fullname or email

}
